name: Hello World

env: 
    MY_VAR: aurélien

# trigger
on: 
    # push: { branches: [main] }
    workflow_dispatch:
    
jobs: 
    sayHello:
        runs-on: windows-latest
        steps: 
            - name: step 1
              run: echo "Hello ${{ vars.NAME }} et $MY_VAR"
  